import 'package:polymer/polymer.dart';

/**
 * A Polymer custom-tab element.
 */
@CustomTag('custom-tab')

class CustomTab extends PolymerElement {

  /// Constructor used to create instance of CustomTab.
  CustomTab.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of custom-tab is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of custom-tab is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// custom-tab is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when custom-tab has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
