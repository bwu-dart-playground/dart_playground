import 'package:polymer/polymer.dart';

/**
 * A Polymer drag-container element.
 */
@CustomTag('drag-container')

class DragContainer extends PolymerElement {

  /// Constructor used to create instance of DragContainer.
  DragContainer.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of drag-container is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of drag-container is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// drag-container is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when drag-container has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
