import 'package:polymer/polymer.dart';

/**
 * A Polymer styled-element element.
 */
@CustomTag('styled-element')

class StyledElement extends PolymerElement {

  /// Constructor used to create instance of StyledElement.
  StyledElement.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of styled-element is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of styled-element is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// styled-element is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when styled-element has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
